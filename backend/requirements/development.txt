# =============================================================================
# Development requirements — lokal ishlash uchun
# =============================================================================
-r base.txt

# --- Dev helpers ---
django-debug-toolbar>=4.4,<6.0
django-extensions>=3.2,<5.0
ipython>=8.27,<10.0
werkzeug>=3.0,<4.0   # runserver_plus

# --- Lint / format / type ---
black>=24.8,<26.0
ruff>=0.6,<1.0
mypy>=1.11,<2.0
django-stubs[compatible-mypy]>=5.0,<6.0
djangorestframework-stubs[compatible-mypy]>=3.15,<4.0

# --- Pre-commit ---
pre-commit>=3.8,<5.0
